name: Deploy DocC

# Only update documentation for official updates to the main branch
on :
  push:
    branches: ["main"]

# Allow deployment on GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Only one deployment at a time
concurrency:
  group: pages
  cancel-in-progress: true
